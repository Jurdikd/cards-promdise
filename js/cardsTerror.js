/* Cards resumen*/
/* Carda del documento*/
window.addEventListener("load", () => {
	card_description.forEach((description) => {
		console.log(description);
	});
});
/* Select every class from card-description*/
const card_description = document.querySelectorAll("card-description");
